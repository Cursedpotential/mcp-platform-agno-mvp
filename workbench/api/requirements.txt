# Byline: Claude Code · Sonnet (agent) · 2026-07-19
# Knowledge Workbench API — staging + promote surface only.
# Deliberately NO torch/transformers/sentence-transformers/openai/langchain:
# this app never chunks, embeds, or runs an LLM itself (see README.md).

fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0
lancedb>=0.15.0
pyarrow>=18.0.0
httpx>=0.27.0

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
