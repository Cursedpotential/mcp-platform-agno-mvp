**Goal**
Advance the tool‑centric evidence platform by validating remaining gaps (SBV parser, audit coverage), updating doc paths, and producing integration decisions for Smart Chunker + Conversation Extractor.

**Step 1 — Locate active workspace + task file**
- Confirm the current repo root and open `.claude/tasks/update-doc-paths-after-restructure.md`.
- Enumerate any referenced paths to update and create a checklist of affected docs.

**Step 2 — SBV parser verification**
- Search the workspace and known tool directories for an SBV parser or utility.
- If found, record path, entrypoint, and usage; if missing, document the gap and propose options (build wrapper vs. external tool).

**Step 3 — Audit hook coverage**
- Scan tool modules for missing `@audit_tool` usage.
- Apply audit decorator consistently and ensure tool execution logging matches `init/postgres/03-tool-execution-log.sql`.

**Step 4 — Smart Chunker & Conversation Extractor deep dive**
- Locate source, identify dependencies, input/output formats, and any missing wrappers.
- Draft integration notes and recommended tool interfaces (inputs/outputs, audit logging, config knobs).

**Step 5 — Decisions on GLiNER / Recognizers‑Text / NLP Toxicity Analyzer**
- Summarize tradeoffs and propose integration tiers (immediate vs. later phase).
- Update `tools/TOOLS_REQUIRING_DISCUSSION.md` accordingly.

**Deliverables**
- Updated `.claude/tasks/update-doc-paths-after-restructure.md` (status + resolved paths).
- SBV parser verification note (found path or explicit gap + recommended approach).
- Audit decorator applied across tool modules.
- Deep‑dive notes for Smart Chunker & Conversation Extractor.
- Revised `tools/TOOLS_REQUIRING_DISCUSSION.md` decisions.

**Risks / Questions**
- Need confirmation of the active workspace path if multiple copies exist.
