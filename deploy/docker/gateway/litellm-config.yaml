# LiteLLM proxy — one OpenAI-compatible endpoint over every provider we hold keys for.
# Keys come from the container env (compose passes them from .env). Add models freely.
# Byline: Claude Code · Fable 5 · 2026-07-08 (restore embed-text -> nv-embed-v1 4096-d; drift broke graphiti search)
model_list:
  - model_name: glm-5.1         # PRIMARY (Ollama Cloud — owner decision 2026-06-11;
    litellm_params:             #  NVIDIA was rate-limiting, now embeddings/rerank/backup only)
      model: openai/glm-5.1
      api_base: https://ollama.com/v1
      api_key: os.environ/OLLAMA_API_KEY
  - model_name: nemotron        # BACKUP LLM (NVIDIA NIM)
    litellm_params:
      model: openai/nvidia/nemotron-3-super-120b-a12b
      api_base: https://integrate.api.nvidia.com/v1
      api_key: os.environ/NVIDIA_API_KEY
  - model_name: kimi-k2.6
    litellm_params:
      model: openai/moonshotai/kimi-k2.6
      api_base: https://integrate.api.nvidia.com/v1
      api_key: os.environ/NVIDIA_API_KEY
  - model_name: embed-text      # nvidia/nv-embed-v1 (4096-d, SYMMETRIC — no input_type)
    litellm_params:              # MUST stay 4096-d: the Graphiti Neo4j graph is embedded
      model: openai/nvidia/nv-embed-v1   # at 4096-d; a dim change breaks vector.similarity.cosine
      api_base: https://integrate.api.nvidia.com/v1   # and would force a full graph re-embed.
      api_key: os.environ/NVIDIA_API_KEY
      # DO NOT swap in asymmetric embedqa models (input_type poisons query-vs-passage
      # retrieval ~3.5x). 2026-07-08: reverted a drift back to llama-nemotron-embed-vl-1b-v2
      # (2048-d asymmetric) that broke graphiti search with a Neo4j dimension mismatch.

  - model_name: groq-llama
    litellm_params:
      model: groq/llama-3.3-70b-versatile
      api_key: os.environ/GROQ_API_KEY
  - model_name: openrouter-deepseek
    litellm_params:
      model: openrouter/deepseek/deepseek-chat
      api_key: os.environ/OPENROUTER_API_KEY
  - model_name: gemini-pro
    litellm_params:
      model: gemini/gemini-2.5-pro
      api_key: os.environ/GOOGLE_API_KEY

litellm_settings:
  drop_params: true             # tolerate provider-specific params
  num_retries: 2

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY   # set in .env
