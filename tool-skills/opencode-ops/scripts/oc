#!/usr/bin/env bash
# oc — thin shell wrapper around oc.py (uv-managed python)
# Byline: Claude Code - Sonnet - 2026-07-20
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python "$SCRIPT_DIR/oc.py" "$@"
