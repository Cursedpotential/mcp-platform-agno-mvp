#!/usr/bin/env bash
# grc — thin shell wrapper around grc.py (uv-managed python)
# Byline: Claude Code - Sonnet - 2026-07-19
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python "$SCRIPT_DIR/grc.py" "$@"
