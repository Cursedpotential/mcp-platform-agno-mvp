# -- Agno Framework -------------------------------------------
agno>=1.3.0

# -- Web Framework --------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# -- Database -------------------------------------------------
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
psycopg>=3.2.0
pgvector>=0.3.0

# -- Configuration --------------------------------------------
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# -- Data Processing ------------------------------------------
pymupdf>=1.25.0
python-docx>=1.1.0

# -- YAML -----------------------------------------------------
pyyaml>=6.0.1

# -- Testing --------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# -- ChatMiner (conversation parsing) -------------------------
sentence-transformers>=3.0.0    # Local embedding model (80MB)
scikit-learn>=1.5.0             # Cosine similarity, clustering
numpy>=1.26.0                   # Array operations

# -- ChatMiner Cloud Providers (optional) --------------------
# openai>=1.50.0                  # OpenAI embeddings
# google-generativeai>=0.8.0      # Gemini embeddings
# requests>=2.32.0                # Ollama/NVIDIA API calls

# -- ChatMiner GUI (optional) --------------------------------
# streamlit>=1.40.0               # Alternative: web-based GUI
